import { Marker } from '@react-google-maps/api';
import React from 'react';

import classes from './Marker.module.scss';

export const Menu: React.FC = () => {
    return <div className={classes.menu}>sss</div>;
};

import React from "react";
import { Button } from "@mui/material";
import classes from "./MainPage.module.scss";

export const MainPage: React.FC = React.memo(() => {
  return <div className={classes.mainBlock}>SWIPER</div>;
});
